<%= form_with(model: betum, local: true) do |form| %>
  <% if betum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(betum.errors.count, "error") %> prohibited this betum from being saved:</h2>

      <ul>
      <% betum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_area :phone %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.datetime_select :age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
